# Maker Simulator 原型设计

用于游戏列表、游戏菜单、游玩、存档和结果页面的 Desktop Web 与 Mobile Web 原型。

## 视觉基调

- 白底黑字、轻量边框、大标题和胶囊按钮。
- 用柔和的大色块承载游戏介绍和叙事内容；属性、效果和存档信息保持紧凑。
- 普通卡片保持平面感，弹窗使用遮罩和少量阴影。

## 基础样式

### 配色

| 用途 | 色值 |
| --- | --- |
| 页面、文字、主按钮 | 白 `#FFFFFF`、黑 `#000000` |
| 分隔线、浅色底 | `#E6E6E6`、`#F7F7F5` |
| 游戏与叙事色块 | 青柠 `#DCEEB1`、浅紫 `#C5B0F4` |
| 等待与已激活效果 | 奶油 `#F4ECD6`、薄荷 `#C8E6CD` |
| 提示与补充色块 | 浅粉 `#EFD4D4`、珊瑚 `#F3C9B6` |
| 深色面板 | 深蓝 `#1F1D3D`，搭配白字 |
| 数值增减、危险操作强调 | 绿 `#1EA64A`、洋红 `#FF3D8B` |

每张游戏卡或叙事面板使用一种底色，正文主要通过字号和字重区分层级。

### 字体

标题和正文使用 Inter / 系统无衬线字体；数值和辅助标签使用 JetBrains Mono / 系统等宽字体。

| 用途 | Desktop Web | Mobile Web |
| --- | --- | --- |
| 页面大标题 | 64–86px | 48px |
| 叙事标题 | 40–58px | 32–40px |
| 卡片与分组标题 | 24–28px | 22–24px |
| 正文 | 16–20px | 16–18px |
| 按钮 | 16px | 16px |
| 辅助信息 | 12–14px | 12–14px |

大标题偏轻，关键名称加粗；标题行高约 1.1，正文行高 1.45–1.6。

### 间距与形状

- 常用间距：4、8、12、16、24、32、48px；大区块之间留 48–96px。
- 小卡片内边距 16–24px，大面板 32–48px；移动端面板内边距 24px。
- 小卡片圆角 8px，大面板圆角 24px；按钮和标签为胶囊形，图标按钮为圆形。
- 分隔线 1px；按钮高约 44px，左右内边距 16–20px。

## 组件

| 组件 | 原型中的表现 |
| --- | --- |
| 页面栏 | 左侧项目名称和 GitHub 入口，右侧返回或页面操作；白底，高约 56px。 |
| 按钮 | 主操作黑底白字，次操作白底黑字加细边框，辅助操作纯文字；危险操作加洋红强调。 |
| 内容卡片与面板 | 游戏卡包含名称、简介、版本和入口；菜单与结果页用大色块突出主要内容。 |
| 标签与状态条 | 小胶囊显示回合、版本和状态；加载用奶油底、错误用浅粉底、空状态用浅灰底。 |
| 属性行与效果卡 | 属性名称在左、数值在右，变化量显示正负值；效果卡区分已激活和待激活，并展示说明与激活按钮。 |
| 事件入口与叙事面板 | 事件入口横排，选中项加黑边和浅紫底；详情依次展示事件名、节点标题、正文和选项。 |
| 选择与数量控件 | 单选项为纵向按钮列表；多选项显示说明及“− 数量 / 上限 ＋”，提交操作放在列表末尾。 |
| 存档与检查点 | 存档卡、时间线和检查点分层排列，用缩进、连线和标签表达分支；预览在卡片下方展开。 |
| 确认弹窗 | 白色圆角面板、黑色半透明遮罩，显示标题、简短说明、取消和确认按钮。 |

## 响应式

只保留两种布局，以 960px 为分界。原型画板使用 Desktop Web 1440 × 900、Mobile Web 390 × 844。

| 项目 | Desktop Web（≥ 960px） | Mobile Web（< 960px） |
| --- | --- | --- |
| 页面容器 | 普通页面居中，最大宽度 1280px，左右留白 48px。 | 单列，普通内容左右留白 24px；大色块可贴边，贴边时取消圆角。 |
| 游戏列表 | 游戏卡两列排列。 | 游戏卡单列排列。 |
| 菜单与结果 | 大色块内展示标题、简介或结果，按钮横排。 | 内容纵向排列，主要按钮全宽。 |
| 游玩页面 | 左侧约 30% 展示属性和效果，右侧约 70% 展示事件与正文；各长内容区独立滚动。 | 按事件详情、属性、效果的顺序纵向排列，随页面滚动。 |
| 游戏操作 | 放在右侧主内容底部，主要操作突出。 | 主操作全宽，其余按钮换行或纵向排列。 |
| 存档页面 | 左侧存档列表，右侧时间线与预览。 | 存档列表在上，时间线和预览在下，缩小分支缩进。 |
| 弹窗 | 居中，最大宽度 520px，按钮靠右横排。 | 左右留白 24px，按钮纵向排列并占满面板宽度。 |

事件入口较多时可横向滑动；长正文与选项换行，页面内容保持在屏幕宽度内。

## 交互反馈

- 悬停轻微变色，选中加边框或更换底色，禁用置灰。
- 执行操作时保留当前内容，在操作附近显示加载或错误提示。
- 弹窗、预览展开和内容切换可用约 200ms 的淡入淡出。
